{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug app",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/apple_engine",
      "cwd": "${workspaceFolder}/bin",
      "preLaunchTask": "Build",
      "stopOnEntry": false
    }
  ]
}